plugins {
    id 'groovy'
    id 'java-gradle-plugin'
    id 'maven-publish'
}

group = 'com.viliussutkus89.gradle_plugins'
version = '1.0.0'

gradlePlugin {
  plugins {
    assetKeyGenerator {
      id = group + '.' + rootProject.name
      implementationClass = group + '.' + rootProject.name.replace('-', '_') + '.AssetKeyGeneratorPlugin'
    }
  }
}
